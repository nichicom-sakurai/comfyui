# AGENTS.md

Operating guide for AI agents working in this repository.

## Overview

This repository is a mise-managed workspace for running ComfyUI locally on Apple Silicon via comfy-cli. It contains no application source code of its own: it pins the toolchain, provides canonical mise tasks, and keeps git-managed workflow JSON files. The ComfyUI checkout itself lives in `workspace/` (gitignored).

Installed versions (Python and comfy-cli: verify with `mise run doctor`; ComfyUI: see `workspace/comfyui_version.py`):

- Python 3.13.13 (pinned in `mise.toml`)
- comfy-cli 1.12.0 (pinned in the `bootstrap` task, installed in `.venv/`)
- ComfyUI 0.27.1 (installed into `workspace/` by `setup-comfyui`; changes on reinstall)
- ComfyUI-Manager: integrated as the `comfyui_manager` pip package in `.venv/`, not a `custom_nodes/` entry

## Setup

```bash
mise trust
mise install
mise run setup-comfyui
```

`setup-comfyui` runs `bootstrap` (creates `.venv/` with comfy-cli), then installs ComfyUI into `workspace/` (and ComfyUI-Manager into `.venv/` as a pip package) with `--m-series --fast-deps`. It regenerates `override.txt` and `requirements.compiled` (both gitignored). ComfyUI's Python runtime dependencies (torch etc.) are installed into the root `.venv/`.

## Run & Stop

| Action | Command |
| --- | --- |
| Foreground launch | `mise run launch` |
| Background launch | `mise run start` |
| Stop background instance | `mise run stop` |
| Background logs | `.venv/bin/comfy --workspace workspace logs --tail 200` |

The server listens on <http://127.0.0.1:8188>. Prefer `mise run start` / `mise run stop` for agent-driven generation, and leave the server running between generations within one session.

## Health Check

```bash
mise run doctor                              # toolchain + workspace paths
curl -s http://127.0.0.1:8188/system_stats   # server alive? (JSON: versions, devices, VRAM)
curl -s http://127.0.0.1:8188/queue          # queue state
```

If port 8188 is already in use, an instance is probably still running: try `mise run stop` before launching another.

## Directory Layout

| Path | Git | Description |
| --- | --- | --- |
| `mise.toml` | tracked | Toolchain pin + canonical tasks |
| `AGENTS.md` / `CLAUDE.md` | tracked | This guide (`CLAUDE.md` is a symlink — always edit `AGENTS.md`) |
| `README.md` | tracked | Human-facing guide (Japanese) |
| `workflows/` | tracked | Verified API-format workflow JSON files |
| `.claude/` | tracked | Claude Code project settings (`settings.json`) and skills (`skills/`) |
| `.venv/` | ignored | comfy-cli + ComfyUI Python runtime (torch etc.) |
| `workspace/` | ignored | ComfyUI checkout: `models/`, `custom_nodes/`, `input/`, `output/`, `user/` |
| `override.txt`, `requirements.compiled` | ignored | Regenerated by `setup-comfyui` |

Generated images land in `workspace/output/`. Input images for img2img belong in `workspace/input/` (or upload them via the API).

## Canonical Commands

Run every command from the repository root — permission rules in `.claude/settings.json` match these exact prefixes.

- mise tasks: `mise run <bootstrap|setup-comfyui|launch|start|stop|doctor>`; discover with `mise tasks`
- comfy-cli: `.venv/bin/comfy --workspace workspace <subcommand>` — always pass `--workspace workspace` exactly in this relative form (it resolves from the repo root, and permission rules match this literal prefix; an embedded `$PWD` would disable prefix matching)
- ComfyUI HTTP API: `curl -s http://127.0.0.1:8188/<endpoint>` — write the URL immediately after `-s`, and put all other curl flags after the URL (e.g. `-X POST -d @file`, `-o out.png`) so the allowlisted prefix matches; wrap URLs containing `?` or `&` in double quotes. One localhost URL per curl invocation — never chain additional URLs or `--next`.
- comfy-cli permission rules enumerate approved subcommands on the canonical form above; anything not allowlisted prompts. Notable prompting examples: `node uninstall`, `node disable`, `node restore-snapshot`, `model remove`, workspace-level `install` / `update`, `generate`, `publish`, `feedback`, top-level `--where cloud`. Ask the user before running them.
- Never pass `--where` or `--host`/non-local hosts on allowlisted comfy subcommands (`run`, `validate`, `jobs`, `download`, `upload`, `logs`), and never pass extra server args to `launch` (e.g. `-- --listen 0.0.0.0`) — the allowlist cannot block mid-argument flags, so this is a hard convention; ask the user first if remote routing is ever needed.
- comfy-cli emits a JSON envelope (`{"schema": "envelope/1", ...}`) when not attached to a TTY; read `data` / `error` from it

## Working with ComfyUI (Skills)

| Skill | Use for |
| --- | --- |
| `comfyui-workflow` | Creating or editing workflow JSON (API format) |
| `comfyui-run` | Executing a workflow and collecting output images |
| `comfyui-assets` | Downloading models, managing custom nodes |
| `comfyui-docs` | Looking up official documentation (docs.comfy.org/ja) |

Standard image-generation flow: ensure a suitable model exists (`comfyui-assets`) → ensure the server is running → build or patch a workflow (`comfyui-workflow`) → execute and collect outputs (`comfyui-run`) → report the generated file path under `workspace/output/`.

## Conventions & Boundaries

- ComfyUI and custom-node code in `workspace/` is read-only ground truth: never modify it. Reading it (`server.py`, `nodes.py`, `script_examples/`, `openapi.yaml`) to verify behavior is encouraged. The data directories (`models/`, `input/`, `output/`, `user/`) are written only through the flows described in the skills.
- Never delete or rebuild `workspace/` or `.venv/` without an explicit user request; reinstalling costs a multi-GB download. `mise run setup-comfyui` is the sanctioned (re)install path and runs without a prompt — still run it only when the user explicitly asks.
- Keep git-managed workflow JSONs in `workflows/` in API format (see the `comfyui-workflow` skill). `workspace/user/` is ComfyUI's own save area — do not manage it.
- AI-facing docs (this file, skill bodies) are English; `README.md` is Japanese.
- Secrets: pass Hugging Face / Civitai tokens via environment variables or `.venv/bin/comfy auth`; never write them into this repository.
- Commits follow Conventional Commits with Japanese descriptions (no scope enum configured).
- `.claude/settings.json` allow rules take effect after accepting the workspace trust dialog once per machine; put personal overrides in `.claude/settings.local.json` (gitignored).
